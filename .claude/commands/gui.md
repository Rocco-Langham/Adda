Build the Adda GUI and launch it.

1. Run `bash build.sh` from the project root to compile both `adda` and `adda-gui`.
2. If the build succeeds, launch `./adda-gui.exe` in the background.
3. Report whether the build and launch succeeded.
