# Recursion.
define factorial with [n]
    if [n] <= 1
        return 1
    end
    return [n] * call factorial with [n] - 1
end

[i] = 1
while [i] <= 10
    print [i]! = {call factorial with [i]}
    [i] = [i] + 1
end
